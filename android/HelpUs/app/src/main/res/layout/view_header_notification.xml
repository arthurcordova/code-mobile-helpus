<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/notification_view"
    android:layout_width="match_parent"
    android:layout_height="48dp"
    android:background="@color/safetyColorGreen"
    android:orientation="vertical"
    android:visibility="gone">

    <ImageView
        android:id="@+id/notification_view_icon"
        android:layout_width="44dp"
        android:layout_height="match_parent"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:layout_marginStart="16dp"
        android:padding="10dp"
        android:src="@drawable/ic_check_circle" />

    <TextView
        android:id="@+id/notification_view_text"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginEnd="16dp"
        android:layout_toEndOf="@+id/notification_view_icon"
        android:fontFamily="sans-serif-medium"
        android:gravity="start|center"
        android:text="Usuário cadastrado com sucesso!"
        android:textColor="#fff"
        android:textSize="14sp" />

</RelativeLayout>